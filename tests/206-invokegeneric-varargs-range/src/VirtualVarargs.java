import java.util.Arrays;

public class VirtualVarargs {
    public String toString(boolean... elements) {
      return Arrays.toString(elements);
    }

    public String toString(byte... elements) {
      return Arrays.toString(elements);
    }

    public String toString(char... elements) {
      return Arrays.toString(elements);
    }

    public String toString(short... elements) {
      return Arrays.toString(elements);
    }

    public String toString(int... elements) {
      return Arrays.toString(elements);
    }

    public String toString(long... elements) {
      return Arrays.toString(elements);
    }

    public String toString(float... elements) {
      return Arrays.toString(elements);
    }

    public String toString(double... elements) {
      return Arrays.toString(elements);
    }

    public String toString(Object... elements) {
      return Arrays.toString(elements);
    }
  }
